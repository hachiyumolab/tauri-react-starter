# CLAUDE.md — プロジェクト開発ルール（テンプレート）

このファイルは、このプロジェクト固有の開発ルールです。
新しいアプリを始めるときは、`{{ }}` の部分を埋めて自分のプロジェクト用に書き換えてください。
（グローバルな個人ルール `~/.claude/CLAUDE.md` があれば、それに加えて適用されます。）

---

## 🚀 最初の起動時（kickoff）
**未設定のテンプレート状態のときは、まず開発者に立ち上げ面接（kickoff）を提案すること。**

- 判定：アプリ名がまだ仮（`src-tauri/tauri.conf.json` の `productName` が `MyApp`、または `identifier` が `com.example.myapp` のまま）なら「テンプレート未設定」とみなす。
- そのときは、いきなり作業に入らず、こう声をかける：
  > 「このプロジェクトはまだテンプレートの初期状態です。一緒に立ち上げの壁打ちから始めませんか？ `/kickoff` で開始できます。」
- 開発者が同意したら [`.claude/commands/kickoff.md`](.claude/commands/kickoff.md) の手順で進行する（`/kickoff` と同じ）。
- 設定済み（アプリ名が置換済み）なら、この案内は不要。通常どおり開発を進める。

---

## プロジェクト概要（1行）
{{このアプリが何なのかを1行で書く}}

- 企画：[`README.md`](README.md)
- 要件（何を作るか）：[`docs/requirements.md`](docs/requirements.md)
- 設計（どう作るか）：[`docs/architecture.md`](docs/architecture.md)
- 変更履歴：[`CHANGELOG.md`](CHANGELOG.md)

---

## 技術スタック
| 領域 | 技術 |
|---|---|
| アプリ枠 | Tauri v2（Mac / Windows / Linux、将来モバイル） |
| UI | TypeScript + React + Vite |
| 中核ロジック | Rust |
| データ | SQLite（`rusqlite`） |

---

## コーディング規約
- コードには **わかりやすい日本語コメント** をつける。
- 命名や書き方は、周囲の既存コードのスタイルに合わせる。
- 不要な `console.log` やデバッグコードを残さない。
- 色は生のhexを直書きせず、必ず **デザイントークン（CSS変数）経由** で参照する。
- {{このアプリで特にテストを厚くしたい処理があれば書く（例：お金・日付の計算など）}}

---

## 開発の進め方
- 大きな変更の前に、何をするかを先に説明する。
- 疑問があれば勝手に進めず、必ず質問する。
- 調査・デバッグはサブエージェントを活用してコンテキストを節約する。
- 会話が長引く前に、重要点をこのリポジトリ内のmdへ定期的に保存・更新する。

---

## 禁止事項
- `git push --force` は絶対にしない。
- `git push` は必ず確認を取ってから（勝手にしない）。
- `rm -rf` などの一括削除はしない。
- `.env` の内容を会話やコードに出力しない。
- main ブランチへ直接コミット・プッシュしない（必ずブランチを切る）。
- `npm install -g` などグローバルインストールは事前確認する。
- `package.json` の依存を無断で追加・削除しない。
- データベースの DROP・TRUNCATE・破壊的マイグレーションは必ず確認してから。
- APIキーや秘密情報をコードにハードコードしない。
- 頼まれていないリファクタリングや整理はしない。

---

## Git / 秘密情報
- コミットメッセージは **1行の日本語でシンプルに**。
- `.env`・DBファイル（`*.sqlite` 等）はコミットしない（`.gitignore` 済み）。
