# 変更履歴（CHANGELOG）

このプロジェクトの主な変更を、新しいものから順に記録します。
バージョンは「大きな変更 . 機能追加 . 修正」の3つの数字で表します（例：`0.1.0`）。
分類は 追加 / 変更 / 修正 / 削除 を使います。

---

## [Unreleased]（未リリース）

### 追加
- Tauri v2（React + TypeScript）＋ Rust ＋ SQLite のスターターテンプレートを用意。
- アプリシェルUI（サイドバー・レスポンシブ・ライト/ダークテーマ）。
- デザインシステム（`src/index.css` のトークン、`docs/design.md`、`docs/styleguide.html`）。
- データ層のサンプル（`items` テーブル・マイグレーション・一覧取得コマンド `list_items`）。
- 学習用ドキュメント（`docs/glossary.md`）と、要件/設計の穴埋めテンプレ。
